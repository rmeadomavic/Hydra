"""Canned RF data fixtures for tabletop demos.

Fixtures are plain JSONL files describing a scene over time — consumed by
``hydra_detect.rf.replay_source.KismetReplaySource``. See that module's
docstring for the schema.
"""
